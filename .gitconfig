[user]
    name = Fredrik Persson
    email = mail@fredrik-persson.se
[core]
    editor = vim
    pager = less -FR
[alias]
    hist = log --graph --format=format:\"%C(red)%h%C(reset) %C(yellow)%ad%C(reset) | %s %C(green)\\[%an\\]%C(reset)%C(bold blue)%d%C(reset)\" --abbrev-commit --date=short
    b = branch -vv
    st = status
